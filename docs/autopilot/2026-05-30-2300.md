# ActionBridge Night Autopilot — 2026-05-30 23:00

## Scope checked
- Repo: `actionbridge-by-schwarzwald-agent`
- Branch: `main`
- Reviewed `ACTIONBRIDGE_GOAL.md`, `README.md`, production readiness/blocker docs, latest autopilot note, TODO/blocker markers, package gates, and current `git status`.
- Working tree contained prior uncommitted bridge-handshake work plus untracked `src/frontend/app/dashboard/*`. Dashboard WIP was treated as out-of-scope prior work because Schwarzwald-Agent integration remains delayed until standalone ActionBridge DoD.

## Blocker selected
Customer-site bridge embeds still needed a clean standalone handshake path: the bridge script must not default customer pages to `/api/actionbridge/bridge/handshake` on the customer origin, and the ActionBridge handshake route must answer browser CORS preflight/denial responses without weakening setup-token origin locks.

## What changed
- Bridge script endpoint resolution now uses, in order:
  1. explicit `data-endpoint`;
  2. same ActionBridge origin derived from the loaded script `src`;
  3. legacy relative fallback only as last resort.
- Setup-session bridge snippet now pins the handshake API endpoint to the ActionBridge origin while keeping the shown-once setup-token placeholder.
- Bridge handshake route now supports `OPTIONS` preflight and attaches bounded CORS headers to rate-limit denials, fail-closed error branches, and successful handshakes.
- CORS origin reflection is restricted through `normalizeActionBridgeHandshakeOrigin(...)`: HTTPS public origin only, no private/internal hosts, origin-only format.
- Existing controls remain intact: credentials omitted, token digest lookup only, `Origin`/body origin mismatch rejection, setup-link target-origin match, status/expiry checks, connector binding recheck, revoked-install block, and connected audit.
- Userflow smoke now fetches `/actionbridge/bridge.js`, verifies endpoint-resolution markers, checks bridge-handshake `OPTIONS`, and checks invalid-token POST still returns CORS for customer-site embeds.
- Demo/static contracts now assert endpoint pinning/fallback and CORS route markers.

## Reviews
- Sentinel quick review: GO; Critical/High `0/0`. Notes: CORS reflection is bounded by public HTTPS origin normalization, no credentials, and POST origin-lock/token checks. Required guardrails remain high-entropy expiring tokens, no token logging, generic error posture, denial/success audit monitoring, and controlled-pilot scope.
- Nexus quick review: GO; Blocker/Critical `0/0`. Notes: endpoint pinning + script-src fallback are suitable for customer-site embeds; CORS helper placement covers preflight, error branches, rate limits, and success. Recommendations: add more explicit POST negative CORS tests later, keep dashboard WIP excluded, consider replacing the legacy relative fallback with a loud failure later.

## Verification run
- `npm run test:contracts`
- `npm run test:demo-flow`
- `npm run typecheck`
- `npm run lint`
- `npm run check`
  - includes `npm test`, `typecheck`, `lint`, `build`, `test:userflow-smoke`, `audit:high`, `git diff --check`
  - result: green; dependency audit remains `high: 0`, `critical: 0`, `moderate: 2`.

## Commit / push note
- This run commits only the standalone ActionBridge bridge-handshake slice and this autopilot record.
- Local untracked dashboard integration WIP remains intentionally uncommitted/out-of-scope.
- Branch was already ahead of `origin/main` by one local commit at run start; pushing after this run will push both the prior local setup-activation commit and this verified bridge-handshake slice.

## Remaining known production hardening
- Production/broad rollout remains blocked by the managed Secret Manager/KMS evidence package, least-privilege runtime identity/token issuance, live redacted evidence, and final Sentinel release review.
- Deployed staging SSRF/DNS/rebinding/receiver smoke evidence is still required before production.
- Add explicit route-level negative CORS tests for invalid/missing Origin, Origin/body mismatch, and rate-limit CORS headers when this path is hardened beyond controlled pilot.
