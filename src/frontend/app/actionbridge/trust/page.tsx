const controls = ['Domain ownership verification', 'Digest-only setup tokens', 'Exact Origin bridge handshake', 'Server-owned allowlists', 'Read-only execution gates', 'Approval for draft/write actions', 'Redacted audit logs', 'Kill switch before execution'];
export default function ActionBridgeTrustPage(){return <main className="min-h-screen bg-[#06120f] px-6 py-12 text-stone-100"><section className="mx-auto max-w-5xl"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-teal-300">Trust Center</p><h1 className="mt-4 text-4xl font-black">ActionBridge protects the customer boundary before agents can act.</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{controls.map((c,i)=><div key={c} className="rounded-3xl border border-teal-200/15 bg-teal-300/10 p-5"><span className="text-xs text-teal-200">0{i+1}</span><h2 className="mt-2 font-bold">{c}</h2></div>)}</div></section></main>}
