const failures = [['Falsche Domain','Origin mismatch: bridge handshake blocked.'],['Abgelaufener Link','Setup session closed; operator creates a new link.'],['Verifikation fehlt','Capabilities cannot be enabled.'],['Capability blockiert','Tool does not appear in agent catalog.'],['Approval fehlt','Draft/write execution stops before action.']];
export default function ActionBridgeFailuresPage(){return <main className="min-h-screen bg-[#130f08] px-6 py-12 text-stone-100"><section className="mx-auto max-w-5xl"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-amber-300">Failure-State UX</p><h1 className="mt-4 text-4xl font-black">When something is unsafe, the product says why and stops.</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{failures.map(([title,body])=><article key={title} className="rounded-3xl border border-amber-200/15 bg-amber-300/10 p-5"><h2 className="font-bold text-amber-100">{title}</h2><p className="mt-2 text-sm text-stone-300">{body}</p></article>)}</div></section></main>}
