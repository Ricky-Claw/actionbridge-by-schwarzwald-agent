# ActionBridge Night Autopilot — 2026-05-29 08:19 Europe/Berlin

## Scope checked
- Read `ACTIONBRIDGE_GOAL.md`, `README.md`, production readiness/blocker docs, behavioral roadmap, and latest 2026-05-29 03:00 autopilot review artifacts.
- Git started clean on `main...origin/main` before this slice.
- Selected one standalone DoD blocker: bound setup links did not prove that the chosen connector belongs to the same HTTPS origin as the customer setup target, and bridge handshake did not recheck that binding for legacy/mismatched rows.

## Blocker fixed
- Added server-side setup-link origin binding helper in `src/frontend/lib/actionbridge/setup-links.ts`:
  - normalizes setup target origins as HTTPS origin-only, no credentials, no private/local/internal hosts, no path/query/hash;
  - normalizes connector `base_url` / `allowed_origins` to safe HTTPS origins;
  - fails closed when connector base origin is invalid/private/http;
  - verifies the setup target origin is exactly one of the connector-owned origins.
- Updated `POST /api/actionbridge/setup-links` to verify optional connector bindings before token issuance, reject mismatches with `ACTIONBRIDGE_SETUP_LINK_CONNECTOR_ORIGIN_MISMATCH`, and persist denied audit evidence for origin-mismatch attempts.
- Updated `POST /api/actionbridge/bridge/handshake` to recheck bound connector origin before creating/completing an installation, reject missing/mismatched bindings with `ACTIONBRIDGE_BRIDGE_CONNECTOR_BINDING_NOT_FOUND` / `ACTIONBRIDGE_BRIDGE_CONNECTOR_ORIGIN_MISMATCH`, and persist denied audit evidence for origin-mismatch handshakes.
- Kept public route modules free of direct raw `base_url` exposure by moving the connector lookup behind the server-only setup-links helper.
- Updated operator setup-link UI copy and production readiness checklist to document owner-scoped, origin-locked bindings and bridge-only preview for unbound links.

## Security review
- Nexus implementation/architecture review requested. Final Nexus review returned **GO** with no severity findings after the helper refactor; evidence noted that the raw `base_url` / `allowed_origins` lookup is encapsulated in the server-only setup-links helper and route leak checks pass.
- Sentinel security review requested. Sentinel returned **GO** with `Critical: 0 | High: 0`, one Medium audit-gap finding, and one Low test-drift finding. The Medium was fixed in this slice by adding denied audit events for setup-link and handshake origin mismatches. Final Sentinel re-review after helper refactor hit provider permission `401` before producing a result.
- Breaker self-review result after Sentinel fix: no High/Critical blocker found. The change is fail-closed for origin mismatch, owner-scoped at setup-link creation, service-scoped plus setup-link owner at bridge handshake, token digest remains server-side, denied attempts are audited, and routes do not return connector base URLs or secrets.

## Verification
- `npm run test:contracts` ✅
- `npm run test:security` ✅
- `npm run test:behavioral-security` ✅
- `npm run test:demo-flow` ✅
- `npm run typecheck` ✅
- `npm run lint` ✅
- `npm run check` ✅ (`npm test`, `typecheck`, `lint`, `build`, `test:userflow-smoke`, `audit:high`, `git diff --check`)
- `npm run audit:high` reported `{ "high": 0, "critical": 0 }` ✅

## Files changed
- `src/frontend/lib/actionbridge/setup-links.ts`
- `src/frontend/app/api/actionbridge/setup-links/route.ts`
- `src/frontend/app/api/actionbridge/bridge/handshake/route.ts`
- `src/frontend/app/actionbridge/operator/ActionBridgeSetupLinksClient.tsx`
- `scripts/test-actionbridge-behavioral-security.mjs`
- `scripts/test-actionbridge-security-gauntlet.mjs`
- `scripts/test-actionbridge-demo-flow.mjs`
- `docs/production-readiness-checklist.md`

## Remaining blocker
- Production Secret Manager/KMS/IAM/live evidence remains external environment work and is still the production launch blocker.
