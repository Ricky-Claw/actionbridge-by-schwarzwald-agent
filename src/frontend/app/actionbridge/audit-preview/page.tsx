const events = ['setup_link.created', 'domain_verification.challenge_issued', 'bridge.handshake.connected', 'capability_rule.enabled', 'agent_tools.catalog_listed', 'execution.approval_required', 'audit.redacted_event_recorded'];
export default function ActionBridgeAuditPreviewPage(){return <main className="min-h-screen bg-zinc-950 px-6 py-12 text-zinc-100"><section className="mx-auto max-w-4xl"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-emerald-300">Audit Timeline</p><h1 className="mt-4 text-4xl font-black">Security becomes visible, not hidden in logs.</h1><ol className="mt-8 space-y-4">{events.map((event,i)=><li key={event} className="flex gap-4 rounded-3xl border border-white/10 bg-white/[0.04] p-4"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-emerald-300 font-black text-zinc-950">{i+1}</span><div><h2 className="font-mono text-sm text-emerald-100">{event}</h2><p className="mt-1 text-sm text-zinc-400">Redacted, tenant-scoped, and useful for customer trust.</p></div></li>)}</ol></section></main>}
