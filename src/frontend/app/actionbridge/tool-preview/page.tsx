const tools = [['site.knowledge.read','read','No approval','Answer from approved public knowledge.'],['lead.prepare_draft','write','Approval required','Prepare contact draft only.'],['appointment.request.prepare_draft','write','Approval required','Prepare appointment request only.']];
export default function ActionBridgeToolPreviewPage(){return <main className="min-h-screen bg-slate-950 px-6 py-12 text-slate-100"><section className="mx-auto max-w-5xl"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-cyan-300">Agent Tool Preview</p><h1 className="mt-4 text-4xl font-black">Show customers exactly what Schwarzwald-Agent receives.</h1><div className="mt-8 grid gap-4 md:grid-cols-3">{tools.map(([name,risk,approval,desc])=><article key={name} className="rounded-3xl border border-cyan-200/15 bg-cyan-300/10 p-5"><h2 className="font-mono text-sm text-cyan-100">{name}</h2><p className="mt-3 text-xs uppercase tracking-widest text-cyan-300">{risk} · {approval}</p><p className="mt-3 text-sm text-slate-300">{desc}</p></article>)}</div><p className="mt-6 text-sm text-slate-400">No base URLs, raw config, token digests, private tenant fields, or secrets are shown.</p></section></main>}
